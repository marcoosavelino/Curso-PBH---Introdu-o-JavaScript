// Construa uma função que receba dois valores de entrada do usuário e faça da soma desses valores.

function soma(a, b){
    return parseInt(a) + parseInt(b);
}

console.log("Resultado: " , soma(5, 6));
